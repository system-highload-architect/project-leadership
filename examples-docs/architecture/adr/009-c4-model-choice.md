# ADR-009: Выбор C4-модели для визуализации архитектуры

**Статус:** Принято  
**Дата:** 2026-07-26  
**Автор:** Tech Lead / Архитектор

---

## Контекст

Для документирования архитектуры E‑Commerce платформы необходим способ визуализации, который:
- Понятен разным аудиториям (бизнес, архитекторы, разработчики, DevOps).
- Позволяет показать систему на разных уровнях абстракции.
- Легко обновляется и хранится в репозитории.
- Интегрируется с инструментами документации (PlantUML, Markdown).
- Поддерживает отображение эволюции системы: от модульного монолита к микросервисам.

Рассматривались:
1. **C4-модель** (Context, Containers, Components, Code)
2. **Диаграммы UML** (все 14 типов)
3. **Архитектурные шаблоны** (4+1 View Model)

---

## Решение

Выбрана **C4-модель** как основной подход для визуализации архитектуры, дополненная **UML-диаграммами** для деталей реализации (Code-уровень).

**Обоснование:**

- **Многоуровневость:** C4 показывает систему от контекста до кода, что удобно для разных стейкхолдеров.
- **Простота:** Нотация интуитивно понятна даже не-техническим специалистам (Context, Containers).
- **Хранение в коде:** Диаграммы описываются текстом (PlantUML) и хранятся в репозитории, что позволяет версионировать изменения.
- **Гибкость:** C4 легко комбинируется с UML (для Component и Code уровней).
- **Инструменты:** Поддержка PlantUML, возможность генерации PNG/SVG для документации.
- **Эволюционность:** C4 позволяет показывать разные варианты архитектуры (монолит, микросервисы, EDA, Serverless) в отдельных наборах диаграмм.

---

## Альтернативы

| Альтернатива | Плюсы | Минусы | Почему не выбрана |
|--------------|-------|--------|-------------------|
| **UML (все типы)** | Полнота, стандарт | Избыточен для высокоуровневого описания, сложен для бизнеса | C4 проще для коммуникации с заказчиком |
| **4+1 View Model** | Разделение на виды (логический, процессный, физический) | Менее структурирован, нет единой нотации | C4 даёт более чёткие уровни абстракции |

---

## Последствия

### Положительные
- ✅ Единый язык для всех участников проекта.
- ✅ Чёткое разделение уровней: Context → Containers → Components → Code.
- ✅ Лёгкость поддержки — диаграммы рядом с кодом.
- ✅ Возможность автоматической генерации в HTML-документацию.
- ✅ Возможность показывать эволюцию системы через отдельные наборы диаграмм (монолит, микросервисы, EDA, Serverless).

### Отрицательные
- ❌ Необходимость обучения команды C4-нотации (быстрое, 1–2 часа).
- ❌ Для Code-уровня требуется дополнительная UML-диаграмма классов (но это уже есть в арсенале).

### Компромиссы
- Используем **C4 для высокоуровневой архитектуры** (Context, Containers, Components).
- Для **Code-уровня** используем **UML-диаграммы классов** (как часть C4-модели).
- Все диаграммы храним в `examples-docs/architecture/diagrams/` с подпапками для каждого паттерна (`monolith/`, `microservices/`, `event-driven/`, `serverless/`).

---

## Связь с паттернами и будущими решениями

| Паттерн / Решение | Как связано с C4 |
|-------------------|-------------------|
| **Модульный монолит** | C4-диаграммы для монолита показывают один контейнер (приложение) с внутренними модулями |
| **Микросервисы** | C4-диаграммы для микросервисов показывают несколько контейнеров (сервисов) с взаимодействием через события и API Gateway |
| **EDA** | C4-диаграммы для EDA показывают брокер событий (Kafka) и поток событий между контейнерами |
| **Serverless** | C4-диаграммы для Serverless показывают функции (FaaS) и события, которые их триггерят |
| **CQRS / Event Sourcing** | C4-компоненты показывают разделение на Command и Query стороны, Event Store и проекции |
| **Saga** | C4-динамические диаграммы показывают последовательность шагов Saga и компенсации |

---

## Связанные ADR

- [ADR-010: Выбор инструментов для диаграмм](./010-diagrams-tools.md) — инструменты реализации C4.
- [ADR-001: Модульный монолит](./001-modular-monolith.md) — C4 для монолита.
- [ADR-003: SOA vs Микросервисы](./003-soa-vs-microservices.md) — C4 для микросервисов.
- [ADR-012: Patterns Choice](./012-patterns-choice.md) — общий выбор паттернов.

---

*Решение пересматривается при переходе на другие инструменты визуализации (например, Mermaid вместо PlantUML) или при изменении требований к визуализации.*